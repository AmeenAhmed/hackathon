# Development Dockerfile with hot-reload
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Expose Vite dev server port
EXPOSE 8081

# Run development server with host flag for Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]